// Shared workspace public key for the voice provider (Vapi).
// One value per workspace — every assistant in this workspace uses it.
// Kept in sync with src/hooks/use-vapi.ts DEFAULT_VAPI_PUBLIC_KEY.
export const DEFAULT_VOICE_PUBLIC_KEY = "b4b4be59-182a-492b-bb0b-55aad26306ad";
